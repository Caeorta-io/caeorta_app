// Sync domain — sync session/chunk value types for the opportunistic sync model.
// Placeholder: add the Zod schema + inferred types when the sync domain is built
// (see docs/07_Sync_Architecture.md).
export {};
