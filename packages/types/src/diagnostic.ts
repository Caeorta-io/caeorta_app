// Diagnostic domain — AI agent `diagnostic_output` schema.
// Placeholder: add the Zod schema + inferred types when the diagnostics integration is built
// (see docs/06_AI_Agent_Contract.md).
export {};
