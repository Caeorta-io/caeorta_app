// Telemetry domain — telemetry row schema (device → cloud → app).
// Placeholder: add the Zod schema + inferred types when the telemetry domain is built
// (see docs/07_Sync_Architecture.md).
export {};
