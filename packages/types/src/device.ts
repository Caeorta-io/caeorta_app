// Device domain — device records, pairing, and provisioning value types.
// Placeholder: add the Zod schema + inferred types when the device domain is built
// (see docs/01_Project_Identity.md § device pairing).
export {};
